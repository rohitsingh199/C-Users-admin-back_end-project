
        <h1>
            Codeial / <%= title %>
        </h1>




        <section id ="feedpost">

            <h4>Post</h4>
            <%if (locals.user){%>
            <form action="/posts/create" id="new-post-form" method="POST">
                <textarea name="content" id="" cols="30" rows="2" placeholder="typr here"></textarea>
                <input type="Submit"  value="Post">            </form>
                <% } %>
                <div id="posts-list-comntainer">
                    <ul>
                        <% for(post of posts){%>
                            <li>
                                <%= post.content %>
                                <!-- <%= post.user %> will show all the info -->
                                <br>
                                <%= post.user.name %>

                            </li>


                            <%}%>
                    </ul>
                
            </section>